generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Project {
  projectId   String   @id @db.VarChar(20)
  name        String   @db.VarChar(100)
  description String?  @db.Text
  created     DateTime @default(now()) @db.Timestamp(0)
  updated     DateTime @default(now()) @db.Timestamp(0)
  tasks       Task[]

  @@map("projects")
}

model Task {
  taskId      String   @id @db.VarChar(20)
  title       String   @db.VarChar(100)
  description String?  @db.Text
  created     DateTime @default(now()) @db.Timestamp(0)
  updated     DateTime @default(now()) @db.Timestamp(0)
  project     String   @db.VarChar(20)
  priority    Int
  projects    Project  @relation(fields: [project], references: [projectId], onDelete: NoAction, onUpdate: NoAction, map: "tasks_ibfk_1")

  @@index([project], map: "project")
  @@map("tasks")
}
